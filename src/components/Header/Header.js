import React from 'react';

import css from './header.module.css'

const Header = () => {
    return (
        <div className={css.Header}>
            Rick & Morty
        </div>
    );
};

export {Header};